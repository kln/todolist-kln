<h2>Minhas Listas</h2>

<% current_user.lists.ordered.each do |list| %>
  <div class="row list-item">
    <%= link_to list.title, list %>
    <%= link_to image_tag('list/delete.png', title: "Excluir"), list, method: :delete, data: { confirm: 'Você tem certeza?' } %>
    <%= link_to image_tag('list/edit.png', title: "Editar"), edit_list_path(list) %>
  </div>
<% end %>

<div class="row">
  <%= link_to "Nova lista", new_list_path, :class => 'btn', :id => 'new-list' %>
</div>